# Student Manager
We built a list of students, grades, and class in the students.csv file.

In the load_students function, we took students.csv and turned it into a dictionary that prints for each student the name, grade, and class in the dictionary.

In the add_student function, we added a new student dictionary to the list that increases by 1.
The find_student function returns a student dictionary by name, if not, it returns NONE.
class average function We selected a specific class and returned the class average

In the top_student function, we built a loop that iterates through the student with the highest score and returns the

In the print_all function, we created a function that prints the names of all students.